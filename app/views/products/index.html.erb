<% if @logged_in_as %>
  <%= link_to "Post new product", :new_product, class: "button new" %>
<% end %>
<% if @products.present? %>
  <% @products.each do |product| %>
    <%= render "shared/product", product: product %>
  <% end %>
<% else %>
  <h1>No product found!</h1>
<% end %>
